CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = clean oclean

all: ${NAME}

${NAME}: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

%.o: %.c
	${CC} -c $< -o $@

clean: 
	rm -f *~ school

oclean:
	rm -f ${OBJ}

fclean: ${RM}

re: oclean ${NAME}
